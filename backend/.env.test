# Configuración para tests (base de datos SQLite en memoria)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=test
DB_PASSWORD=test
DB_DATABASE=test_db

# JWT para tests
JWT_SECRET=test_jwt_secret_key_for_testing_only

# Puerto para tests
PORT=3001
NODE_ENV=test
